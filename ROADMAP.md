# ROADMAP.md

Пошаговый план перехода от текущего боевого прототипа (данж-сеттинг,
без персистентности) к играбельному прототипу с мехами/деталями, а затем
к БД, аккаунтам и прокачке. Основа для планирования — [AGENTS.md](AGENTS.md).

Решения, зафиксированные с пользователем на момент написания плана:

- Система деталей на первом этапе — **минимальная** (корпус, 1-2 слота
  оружия, ноги), без FM3-глубины. Углубление — отдельный этап после того,
  как core уже играбелен.
- Механика "выход из данжа" — **не нужна**, убираем.
- Сундуки — **хотим оставить** в переосмысленном виде: дроп деталей
  (особенно редких) прямо в бою. Если не впишется по геймплею/балансу —
  fallback на случайный дроп после победы в бою (без потери кода, см. 1.3).
- Процедурная генерация уровней — **не приоритет**, курс на ручные карты
  через [static/map_editor.html](static/map_editor.html). Жертвуем
  процедурной генерацией, если она будет мешать рефакторингу.
- Золото — убираем как отдельную сущность, награда после боя = валюта
  + дроп деталей, начисляется на этапе БД (Этап 3).
- Стек для БД — **PostgreSQL + SQLAlchemy/SQLModel**.

## Этап 0 — сделано

- [AGENTS.md](AGENTS.md): описан сеттинг, зафиксировано расхождение
  кода с сеттингом.

## Этап 1 — минимальный меха/детали core (без БД, всё ещё in-memory)

Цель этапа: убрать то, что явно не нужно от данж-сеттинга, и завести
минимальную, но настоящую систему "пилот + меха из деталей" вместо плоских
`CharacterStats`, которые сейчас руками прописаны в [lobby.py](lobby.py).
БД пока не трогаем — лоадаут при старте боя всё ещё выдаётся из
захардкоженного каталога в коде.

1. ✅ **Убрать механику выхода из данжа.** — сделано.
   - Удалены `ActionType.EXIT`, `ActionHandler.__perform_action_exit`,
     `CELL_TYPE.EXIT`, `Dungeon.exits`/`_generate_exits`, спец-случай для
     клетки выхода в `__perform_action_move`, поле `exits` в `dto/state.py`,
     кнопка/рендер/хендлер в [templates/debug_map.html](templates/debug_map.html)
     и палитра в [static/map_editor.html](static/map_editor.html).
     Клетки бывших выходов на `map_1`/`map_2` стали обычным полом.
   - Условие конца игры не зависело от "выхода" и не менялось
     (`Game.check_game_end` считает только смерти команд/врагов).

2. ✅ **Переосмыслить сундуки под дроп деталей.** — сделано (первая версия).
   - `Chest.gold: int` → `Chest.loot: str`. Пока это **заглушка**
     (`PLACEHOLDER_LOOT` в [src/entities/chest.py](src/entities/chest.py)) —
     случайное строковое имя вместо ссылки на реальную деталь, т.к. каталога
     деталей (п. 3) ещё нет. Когда он появится — `loot` меняется на ссылку
     на `Part`/`Weapon` с редкостью, остальная механика не трогается.
   - Реализован `ActionHandler.__perform_action_open_chest` +
     `Dungeon.remove_chest`: подбор возможен только на расстоянии ≤1 клетки
     (Chebyshev) от сундука; сундук исчезает с карты, лут уходит в новое
     поле `Actor.trophies: list[str]` — это и есть тот самый "reward pool"
     на актора, задел под будущую систему наград. Если механика подбора
     в бою не приживётся на плейтесте — переключить источник трофеев на
     пост-матчевую награду можно, не трогая `trophies` и сам расчёт наград.
   - `dto/state.py`: `ChestState` по-прежнему не отдаёт содержимое сундука
     клиенту заранее (раньше — `gold`, теперь — `loot`); `Actor.trophies`
     тоже пока не прокинуто в DTO — это чисто серверная бухгалтерия до
     появления реальной системы наград.
   - Не сделано намеренно: у трофея нет отображения в HTML-клиенте и нет
     влияния на баланс/выдачу конкретных деталей — это тема этапа 2.

3. ✅ **Минимальная модель Pilot / Mech / Part.** — сделано (2026-07-08).
   Решено с пользователем:
   - **Слоты меха на эту итерацию:** `torso` (здоровье), `legs` (скорость),
     `arms` (точность + сила удара рукопашного), `head` (дальность
     видимости) — по одному слоту каждого типа. Два слота под руки
     (левая/правая) и правило суммирования их бонусов — сознательно
     отложено на Этап 2 (см. ниже).
   - **Оружие независимо от рук** на этой итерации: `Inventory.weapons`
     остаётся как есть, руки дают точность/силу удара всему носимому
     оружию, а не какому-то конкретному. Привязка оружия к слоту руки
     (кол-во оружия = кол-во рук) — тоже отложена на Этап 2.
   - **Сила удара рук добавляется к урону оружия ближнего боя**
     (`weapon.type == "melee"`): `итоговый_урон = weapon.damage + actor.stats.melee_power`.
     Дальнобойное оружие не затрагивается. Заодно это чинит то, что
     `CharacterStats.damage` был объявлен, но нигде не учитывался в бою —
     поле переименовано в `melee_power` и наконец применяется.
   - **`Player` не переименовываем в `Pilot`** сейчас — просто добавляем
     ему поле `mech: Mech` (+ заготовки `xp: int = 0`, `level: int = 1`
     под будущую прокачку). Переименование — по-прежнему часть п. 6
     "точечный ребрендинг", отдельной задачей.
   - `Enemy` на этой итерации `Mech` не получает, остаётся на плоских
     `CharacterStats`/`Inventory` (только `damage` → `melee_power` в
     местах создания в [src/arena.py](src/arena.py) (тогда ещё
     `src/dungeon.py`, переименован в п. 7), для единообразия с
     переименованным полем).

   Реализация:
   - [src/entities/part.py](src/entities/part.py) (новый): `PartSlot`
     (`TORSO`/`LEGS`/`ARMS`/`HEAD`) + `Part` (id, slot, name, и
     `health`/`speed`/`accuracy`/`melee_power`/`view_distance`, все `= 0`
     по умолчанию — деталь заполняет только то, что относится к её слоту
     по конвенции, модель этого не форсирует).
   - [src/entities/mech.py](src/entities/mech.py) (новый): `Mech`
     (`torso`/`legs`/`arms`/`head`: `Part`) + метод
     `build_character_stats(action_points: int) -> CharacterStats`,
     суммирующий соответствующие поля всех 4 деталей и добавляющий
     переданные очки действия (единственный стат, который на этой
     итерации остаётся "от пилота", а не от детали).
   - [src/entities/base.py](src/entities/base.py): `CharacterStats.damage`
     → `CharacterStats.melee_power`.
   - [src/action_handler.py](src/action_handler.py): в
     `__perform_action_attack` — бонус силы удара для melee-оружия (см. выше).
   - [src/entities/player.py](src/entities/player.py): добавить
     `mech: Mech`, `xp: int = 0`, `level: int = 1`. Без pydantic-магии
     авто-сборки `stats` из `mech` в валидаторе — рассматривали, но
     отказались: `stats` должен на этой итерации оставаться обычным явным
     полем, потому что restore игры из дампа ([game_state_utils.py](game_state_utils.py))
     передаёт уже "просевший" в бою `stats` (health меньше максимума), и
     авто-пересборка из `mech` при каждой валидации молча затёрла бы
     прогресс боя. Вместо этого — явный вызов
     `mech.build_character_stats(action_points=...)` в каждом месте, где
     создаётся новый `Player` "с нуля" (в отличие от восстановления из
     дампа, где `stats` передаётся как есть).

4. ✅ **Каталог деталей (пока в коде, не в БД).** — сделано (2026-07-08).
   - [src/parts_catalog.py](src/parts_catalog.py) (по аналогии с
     [src/maps/default.py](src/maps/default.py)): по одной стартовой
     детали на слот с текущими числами (torso.health=15, legs.speed=5,
     arms.accuracy=87 + arms.melee_power=5, head.view_distance=5 — те же
     значения, что раньше были захардкожены в `lobby.py`, чтобы сама
     реструктуризация не меняла баланс) + `default_mech()`.
     Побочный эффект, о котором стоит знать: `melee_power=5` раньше был
     мёртвым числом (`damage`, нигде не учитывалось), теперь оно реально
     добавляется к урону ближнего оружия — стартовый "Ударный модуль" (damage 3)
     будет наносить 8, а не 3. Если это нежелательно — обнулить
     `melee_power` у стартовой детали или подобрать баланс отдельно.
   - `lobby.py`: `connect_player` берёт `default_mech()` из каталога,
     считает `stats = mech.build_character_stats(action_points=10)`;
     `Inventory.weapons` собирается как раньше (независимо от меха, см. выше).
   - [dto/state.py](dto/state.py): `CharacherStatsState.damage` →
     `melee_power` (иначе клиент получит невалидную форму статов).
   - [game_state_utils.py](game_state_utils.py):
     `restore_player_from_data` — восстанавливать `mech` из дампа
     (`Mech.model_validate(...)`) и передавать в `Player(...)`;
     переименовать `damage` → `melee_power` при восстановлении `stats`.
   - [templates/debug_map.html](templates/debug_map.html): строка статов
     актора — `damage` → `melee_power` в JS.
   - [test_serialization.py](test_serialization.py) и
     [test_dump_restore.py](test_dump_restore.py) — точечно поправить
     ручное создание `Player(...)` (добавить `mech=`, переименовать
     `damage=`→`melee_power=`), чтобы эти smoke-скрипты не сломались.

5. ✅ **DTO меха для клиента + простой дебаг-просмотр статов/деталей.** — сделано (2026-07-08).
   - [dto/state.py](dto/state.py): добавить `PartState` (`slot`, `name`,
     `health`, `speed`, `accuracy`, `melee_power`, `view_distance` —
     зеркало внутреннего `Part`) и `MechState` (`torso`/`legs`/`arms`/`head`: `PartState`).
     Поле `mech: MechState` добавляется в `PlayerState`, а не в общий
     `ActorState` — у `Enemy` меха нет (см. п.3 этапа 1).
   - [templates/debug_map.html](templates/debug_map.html): минимальный
     просмотр, без билдера и без редактирования — только для отладки:
     1. выбор актора (свой игрок по умолчанию; из видимых на карте, если
        понадобится смотреть и на чужих мехов в рамках уже применяемой
        видимости команды);
     2. блок "итоговые характеристики" — весь `stats` целиком (сейчас в UI
        показаны только HP/AP/melee_power — добавить недостающие
        `accuracy`/`view_distance`/`speed`, раз уж дебажим статы);
     3. блок "детали меха" — по каждой из 4 деталей название и её
        собственные ненулевые статы, чтобы было видно, откуда взялась
        итоговая цифра (например, `speed` целиком от `legs`, `melee_power`
        от `arms` и т.д.).
   - Видимость данных не меняем: `filter_visible_entities_for_team` в
     [lobby.py](lobby.py) как есть — это окно для отладки того, что и так
     уже уходит игроку/команде, а не новая лазейка для подглядывания.
   - Полноценный интерактивный билдер меха — не сюда, это Unity-клиент
     и/или Этап 2, когда появится реальный выбор деталей, а не один
     `default_mech()`.
   - ✅ **Аддендум (2026-07-13): лор пресетов в инспекторе.** `MechPreset`
     ([src/mech_presets.py](src/mech_presets.py)) получил поле
     `description` — художественный текст с псевдо-историей (кем/когда
     создан, для чего) для каждого из трёх пресетов. Чтобы инспектор знал,
     какому пресету принадлежит конкретный мех игрока, `Mech`
     ([src/entities/mech.py](src/entities/mech.py)) получил
     `preset_name: Optional[str]`, который простановился в
     `mech_presets._fresh_copy` — `None` для мехов без пресета
     (`default_mech()`). Прокинуто в `MechState.preset_name` и
     `MechPresetState.description` ([dto/state.py](dto/state.py); второе
     уже отдаётся эндпоинтом `/mech_presets`, использованным для выбора
     пресета в лобби). В [templates/debug_map.html](templates/debug_map.html)
     `refresh_mech_presets()` строит по ответу `/mech_presets` карту
     `имя пресета -> description`, а `render_mech_inspector()` показывает
     блок с описанием над статами, если у меха выбранного актора есть
     `preset_name` с известным описанием.

6. **Ручные карты вместо процедурной генерации.**
   - ✅ Редактор дополнен кистью: панель `#brushes` над сеткой позволяет
     выбрать тип клетки (включая `CHEST`) и красить клик/протаскиванием
     мыши вместо циклического перебора кликом по клетке — см.
     [static/map_editor.html](static/map_editor.html).
   - Сами 2-3 карты в [src/maps/default.py](src/maps/default.py) через
     редактор — осознанно не сделано агентом, это на пользователе.
   - `Arena._procedural_generate` (бывший `Dungeon._procedural_generate`) и
     связанный код (`Room`,
     `_generate_rooms/_generate_chests/_generate_enemies/_generate_exits`)
     не дорабатываем; удаляем по факту, когда/если он начнёт мешать
     дальнейшему рефакторингу. Не трогаем ради самого удаления — только
     если реально мешает.

7. ✅ **Точечный ребрендинг сеттинга (без фанатизма).**
   - `Dungeon`/`DungeonMap` → `Arena`/`ArenaMap` — сделано каскадно: класс,
     файл (`src/dungeon.py` → `src/arena.py`), атрибут `Game.arena`/
     `Lobby`, DTO (`ArenaState`, поле `GameState.arena` — меняет и
     wire-формат WS, учли только debug-клиент), ссылки в
     [templates/debug_map.html](templates/debug_map.html) и
     [static/map_editor.html](static/map_editor.html) (заголовки), тесты
     и smoke-скрипты в корне. `Player` осознанно не переименован в
     `Pilot` — оставлен как нейтральное имя.
   - Заодно переименован один из моков оружия врагов: "Старая сабля" →
     "Повреждённый ударный модуль" (стилистически ближе к сеттингу боевых роботов,
     см. [src/arena.py](src/arena.py)).
   - `Player`/`Actor`/`Turn`/`ActionHandler` — осознанно не трогали,
     уже нейтральны.
   - `dungeon_game` как имя репозитория/пакета — **не трогаем** (решение
     пользователя), обсуждение отдельно не требуется.

8. ✅ **Пресеты мехов (только бэкенд).** — сделано (2026-07-12).
   - [src/parts_catalog.py](src/parts_catalog.py): добавлены именованные
     детали для двух пресетов — `STEELMAN_*` (упор в ближний бой: больше
     здоровья и силы удара, ниже точность/скорость/обзор) и `FIREWORKS_*`
     (упор в стрельбу: выше точность/скорость/обзор, ниже здоровье и сила
     удара). `DEFAULT_*`/`default_mech()` не трогали — используются в тестах
     и остаются как нейтральный/тестовый мех.
   - [src/mech_presets.py](src/mech_presets.py) (новый файл): `MechPreset`
     (`name`, `mech`, `weapons`) — пресет как «мех + оружие под него»,
     а не только набор деталей. `STEELMAN_PRESET` (в ближний бой —
     тяжёлое оружие `Кувалда «SteelMan»` + слабый пистолет для дальней
     дистанции) и `FIREWORKS_MK1_PRESET` (в стрельбу — `Штурмовая
     винтовка «Fireworks»` + слабый клинок на случай ближнего боя).
     `get_random_mech_preset()` возвращает копию случайного пресета со
     свежими `id` у всех деталей/оружия (тот же паттерн, что уже был у
     `default_mech()` — детали разных мехов не должны делить id).
   - [lobby.py](lobby.py): `connect_player` больше не собирает мех/оружие
     руками — берёт `get_random_mech_preset()` и использует его `mech` и
     `weapons` как есть.
   - Не сделано намеренно: выбор пресета игроком (сейчас всегда случайно),
     показ названия пресета в HTML-дебаге и любые DTO-изменения — задача
     была явно ограничена бэкендом; выбор пресета клиентом — тема Unity
     и/или системы аккаунтов (Этап 3).

**Критерий готовности этапа 1:** два игрока могут выбрать один из
пресетных мехов, зайти в лобби, сыграть бой на ручной карте, подобрать
дроп детали из сундука (или он просто падает по итогам боя — в
зависимости от того, как впишется), без единого упоминания "подземелья"
в игровых сообщениях.

## Этап 2 — углубление кастомизации (всё ещё без БД)

Делать только после того, как этап 1 пройден и опробован в игре — иначе
рискуем балансировать систему, которая ещё не проверена на прочность.

1. **Больше слотов/деталей, редкости (common/rare/epic и т.п.), реальные
   компромиссы (например, тяжёлый корпус = меньше скорости).**
   - ✅ **Редкость.** — сделано (2026-07-13). `PartRarity` (`common`/`rare`/
     `epic`) добавлен в [src/entities/part.py](src/entities/part.py) как
     поле `Part.rarity` (по умолчанию `common` — обратная совместимость со
     старыми дампами при восстановлении через `Mech.model_validate`). В
     [src/parts_catalog.py](src/parts_catalog.py) `DEFAULT_*` помечены
     `common`, `STEELMAN_*`/`FIREWORKS_*` — `rare` (именной набор пресета,
     не стартовый). `epic`-тир пока не выдан ни одной детали — не выдумывали
     деталь под тир, которому ещё не с чем сравниться (см. ниже про
     количество деталей). Прокинуто в `dto.state.PartState.rarity` и в
     дебаг-просмотр меха в [templates/debug_map.html](templates/debug_map.html)
     (метка `[rarity]` рядом с именем детали).
   - ✅ **Аудит существующих деталей на реальный компромисс.** — сделано
     (2026-07-13, прогонялось через [balance_sim.py](balance_sim.py)).
     Найдено и исправлено одно реальное несоответствие: `STEELMAN_LEGS` в
     ходе прошлых сессий ручной балансировки win rate дошёл до `speed=6` —
     быстрее, чем "манёвренный" `FIREWORKS_LEGS` (тогда `speed=5`), что
     напрямую противоречило заявленному архетипу "тяжёлый = медленный" (тот
     самый пример из формулировки этого пункта). Возвращено к исходной
     направленности (`STEELMAN_LEGS.speed=4` < `FIREWORKS_LEGS.speed=6`),
     просевший win rate SteelMan vs Fireworks скомпенсирован не статами
     SteelMan (пробовали — легко перекомпенсировать через порог "количество
     попаданий винтовки до убийства", HP/урон в этой игре дискретны и дают
     резкие скачки win rate на пороговых значениях), а небольшим снижением
     точности рук `FIREWORKS_ARMS` (92 → 85, непрерывный рычаг, без порогов).
     Остальные слоты (`torso`: health, `arms`: accuracy/melee_power,
     `head`: view_distance) уже были внутренне непротиворечивы у обоих
     пресетов — правок не потребовалось.
   - ✅ **Третий архетип, "StrikeForce" (медленный/малое HP/дамажный).** —
     сделано и сбалансировано (2026-07-13). Расширили дизайн-пространство
     сверх пары "тяжёлый/медленный" ↔ "лёгкий/быстрый": `STRIKEFORCE_*` в
     [src/parts_catalog.py](src/parts_catalog.py) (rarity `rare`, самое
     низкое HP и самая низкая скорость в игре) + `STRIKEFORCE_PRESET` в
     [src/mech_presets.py](src/mech_presets.py) с единственным оружием —
     рейлганом (без запасного, в отличие от SteelMan/Fireworks: весь лоадаут
     поставлен на один разовый урон). Добавлен в `MECH_PRESETS`. Первая
     прикидка урона рейлгана (damage=11) давала разгром обоих других
     архетипов (66% побед у StrikeForce что против SteelMan, что против
     Fireworks в 3v3 через `balance_sim.py`); срезали урон до 10 — этого
     хватило, чтобы обе пары сошлись к ~49/51 и ~48/52. Другие архетипы из
     обсуждавшихся с пользователем ("быстрый/лёгкий/мало HP",
     "быстрый/лёгкий/обзорный") сознательно не делали — решили ограничиться
     одним новым архетипом за раз.
   - **Зафиксировано с пользователем (2026-07-13): больше частей/архетипов
     мехов на этом этапе не требуется** — текущих трёх пресетов
     (SteelMan/Fireworks/StrikeForce) достаточно. Дальнейшее расширение
     деталей (в том числе `epic`-тир, для которого пока не с чем сравнивать)
     — не приоритет, не начинать без отдельного запроса.
   - **Идея на будущее (2026-07-13, не приоритет, не проектировать заранее):**
     разделение `arms` на левую/правую руку (уже отложено в п.3 ниже) и/или
     отдельный слот под ранец/усилитель (jetpack/booster — доп. подвижность
     или доп. защита сверх базовых 4 слотов). Не начинать без отдельного
     решения пользователя — здесь только зафиксирована сама идея, чтобы не
     потерять её.
2. **Случайный урон по части меха (locational damage).** У каждой детали
   (`torso`/`legs`/`arms`/`head`) — собственное HP, отдельное от общего
   `CharacterStats.health`. При попадании часть тела, в которую пришёлся
   урон, выбирается случайно (веса по частям — решить при реализации,
   например торс чаще конечностей). Если HP детали доходит до 0 — деталь
   считается уничтоженной и **перестаёт давать свой бонус** к статам
   (например, уничтоженные `legs` обнуляют бонус к `speed`, уничтоженный
   `head` — бонус к `view_distance`), но актор не умирает автоматически от
   уничтожения не-торсовой детали — смерть по-прежнему определяется общим
   `stats.health <= 0` (см. `Actor.is_dead`), торс менять не требуется.
   Прицеливание в конкретную деталь игроком — не делаем на этой итерации,
   только случайный выбор при попадании.
   - Зависимость на будущее: после того как оружие будет привязано к
     конкретной руке (см. п.4 ниже), уничтоженная `arms`-деталь должна
     делать привязанное к ней оружие недоступным для атаки — это
     естественное продолжение текущего пункта, но реализуется только
     после п.4, не раньше (сначала нужна сама привязка оружие↔рука).
3. Разделить слот `arms` на два (левая/правая рука) — отложено с Этапа 1
   (2026-07-08). Потребует решить: бонусы точности/силы удара суммируются
   от обеих рук или берётся другое правило; как быть с рукой без детали
   (0 или обязательная деталь в каждом слоте).
4. Привязать оружие (`Inventory.weapons`) к рукам — отложено с Этапа 1
   (2026-07-08): каждая рука как слот под одно оружие, количество оружия
   ограничено количеством рук. Естественно тянет за собой п. 3 (нужно
   сначала решить, сколько рук у меха). После этого пункта — вернуться к
   п.2 (locational damage) и доделать её "будущую зависимость": уничтоженная
   рука делает недоступным привязанное к ней оружие.
5. Балансировка дроп-таблиц сундуков/наград — здесь и решится, остаётся
   ли подбор сундука в бою или переезжает целиком в пост-матчевую награду.
   Тесно связан с п.11 (нейтральный страж лута): именно ценный дроп из-под
   стража — главный кандидат на «редкую деталь в бою».
6. Тесты на сборку меха и расчёт итоговых статов (юнит-тесты, не только
   `tests/test_lobby.py` уровня HTTP).
7. Ревью `SimpleEnemyAI` — стоит проверить, что ИИ-противники с новыми
   статами/оружием всё ещё ведут себя разумно (дистанции атаки, точность
   и т.д.). Важно: логика принятия решений **меняется** в п.11 (страж лута:
   снимаем «идти к ближайшему игроку», добавляем удержание зоны) — этот
   ревью делать с учётом п.11, а не отдельно от него.
8. **Прототип боевой механики скиллов (пассивные проки, идейно как в FM3).**
   Зафиксировано с пользователем (2026-07-10):
   - **Только пассивные проки.** Скилл не активируется игроком, а с некоторым
     шансом срабатывает сам в подходящий момент боя (условно: доп. атака,
     уворот, крит, контратака). Активируемых способностей (трата AP/кулдаун)
     — сознательно **не делаем**: это ближе к углублению боевой системы, а
     фокус проекта — кастомизация, не глубина боя.
   - **Полное меню выбора скилла на level-up (ветки развития, выбор 1 из 2)
     и его персистентность — не сюда.** Это Этап 3+ и целевой Unity-клиент
     (см. Этап 3, п. про скиллы). Здесь задача — только **прототип самой
     механики прока в бою**, чтобы её можно было опробовать: XP/уровней,
     которые её открывают, на этом этапе ещё нет, поэтому набор скиллов
     пилоту на прототипе выдаём **захардкоженным** (по аналогии с
     [src/parts_catalog.py](src/parts_catalog.py)/[src/maps/default.py](src/maps/default.py)).
   - **Дерево скиллов — зафиксировано с пользователем (2026-07-10),
     статичное и одинаковое для всех пилотов** (не случайная генерация
     веток), продублировано как JSON-заглушка в
     [skills_tree.json](skills_tree.json) (с `proc_chance` на глаз, ~раз в
     5-15 ходов) — при реализации брать структуру оттуда, не изобретать
     заново. 3 уровня, на 2 и 3 уровне — выбор одного из двух узлов; выбор
     на уровне 3 зависит от того, что выбрано на уровне 2 (настоящее
     ветвление, а не общий пул):
     - **Уровень 1** — без скилла (стартовый уровень).
     - **Уровень 2** (выбор одного из двух):
       - «Точный выстрел» — шанс на этот бросок атаки: временный баф
         `+15` к `accuracy` (только на текущую атаку, не постоянно).
       - «Усиленный удар» — шанс на этот бросок атаки: временный баф `+3`
         к `melee_power` (только на текущую атаку; при проке складывается
         с обычным бонусом `arms`-детали меха, см. Этап 1 п.3, — это
         независимые источники бонуса).
     - **Уровень 3, ветка «Точный выстрел»** (выбор одного из двух):
       - «Двойной выстрел» — шанс выполнить атаку дважды за одно действие.
       - «Бесплатный выстрел» — шанс, что атака не тратит action points.
     - **Уровень 3, ветка «Усиленный удар»** (выбор одного из двух):
       - «Рефлексы» — шанс уклониться от выстрела по овервотчу при движении
         через зону видимости.
       - «Защитная стойка» — шанс поглотить часть входящего урона без
         последствий.
     - **Уточнено с пользователем (2026-07-10): скилл получается на level-up
       и остаётся у пилота навсегда, но его эффект — не постоянный
       модификатор статов.** Каждый скилл — это **проверка шанса каждый ход**
       в момент атаки или защиты (когда актор атакует или когда атакуют
       его); при удачном броске применяется временный баф себе (или дебаф
       противнику) на этот конкретный обмен, не на весь бой. Это относится
       ко **всем** узлам дерева одинаково — и к уровню 2, и к уровню 3,
       единой моделью (никакого разделения на «постоянные» и «проковые»
       скиллы, как ошибочно предполагалось раньше).
     - Конкретные числа (шанс прока, % поглощаемого урона и т.п. для
       уровня 3) — не зафиксированы, подобрать при реализации.
     - **Открытый вопрос, пока не решение, а направление подумать
       (2026-07-12).** Идея «чем сильнее скилл (дальше по дереву), тем
       реже он должен прокать» (см. как это устроено в оригинальной
       Front Mission 3 — там более сильные скиллы занимают больше слотов
       бортового компьютера и от этого реже срабатывают) пользователю
       нравится, но реализовывать её как жёсткое правило пока рано — это
       часть более общей задачи прокачки: скорее всего понадобится общий
       **score силы пилота/сборки**, который одновременно (а) влияет на
       шанс прока скиллов и (б) в перспективе используется для
       **матчмейкинга** — чтобы сильные пилоты не попадали против слабых
       (см. Этап 4, п.3 «Матчмейкинг»). Не проектировать раньше времени:
       сначала посмотреть на прототипе Этапа 2 п.8 (фиксированный
       `proc_chance` без score), как это ощущается в бою, потом решать,
       нужен ли score и как он считается.
   - Что входит в прототип:
     - Модель `Skill` (в `src/entities/`) — как минимум `id`, `name`,
       `level` (2 или 3, для будущей проверки предпосылок дерева при
       реализации выбора на Этапе 3), `trigger` (`ATTACK` — актор атакует,
       `DEFENSE` — актора атакуют), `proc_chance` (шанс сработать при
       наступлении триггера) и эффект — баф себе/дебаф противнику,
       действующий только на текущий обмен атакой. Активной части
       (AP/кулдаун на использование) в модели нет — скилл никогда не
       активируется игроком вручную.
     - Каталог стартовых скиллов `src/skills_catalog.py` (аналог каталога
       деталей) — всё дерево выше как константы, для прототипа выдаём
       пилоту захардкоженным путём по дереву (например, одну ветку целиком)
       вместо реального выбора на level-up (см. ниже, почему).
     - Список скиллов у пилота: `Player.skills: list[Skill]` (у `Enemy`
       скиллов пока нет — по аналогии с тем, что у него нет `Mech`, п.3
       Этапа 1; при желании добавить позже).
     - Хук прока в [src/action_handler.py](src/action_handler.py): в местах,
       соответствующих триггерам (в первую очередь `__perform_action_attack`),
       перед/после расчёта бросить `proc_chance`, и если скилл сработал —
       применить эффект и **отдать это событием** (`GameEvent`), чтобы прок
       был виден в логе боя (сообщение — на русском, как принято в проекте).
     - Овервотч (`OVERWATCH`) — тоже потенциальная точка прока
       оборонительных скиллов; на прототипе достаточно покрыть обычную атаку,
       овервотч можно подключить тем же механизмом, если триггер подойдёт.
   - **Видимость/честность данных:** `Player.skills` — как и `mech`, кладём
     в `PlayerState`, а не в общий `ActorState`, и следим, чтобы список
     скиллов противника не утёк через `filter_visible_entities_for_team`
     в [lobby.py](lobby.py) больше, чем уже утекает лоадаут (server-authoritative,
     см. AGENTS.md). Сам факт сработавшего прока — это уже публичное боевое
     событие (как попадание/промах), его в логе скрывать не нужно.
   - Проверять — уровнем 0 из AGENTS.md: собрать `Player` со скиллом с
     `proc_chance=1.0` (гарантированный прок) и `Action(type=ATTACK)`,
     убедиться, что эффект применился и в `ActionResult`/событиях есть след;
     затем `proc_chance=0.0` — что не применился. Отдельный pytest-файл под
     это не заводим (домен ещё меняется), при желании — smoke-скрипт в корне.
9. **Весовой бюджет меха.** Зафиксировано с пользователем (2026-07-12) как
   основа для тюнинг-слайдеров ниже (п.10): без ограниченного ресурса
   слайдеры бессмысленны — игрок просто выкручивает всё на максимум (по
   мотивам того, как это устроено в Armored Core/BattleTech — грузоподъёмность
   от ног/шасси ограничивает суммарный вес деталей и оружия; в Front Mission 3
   аналогично, но лимит был у корпуса).
   - У `Part` и `Weapon` появляется `weight`. У меха — суммарная
     грузоподъёмность (`weight_capacity`), считается по той же схеме, что
     `Mech.build_character_stats` — от какой именно детали брать базовое
     значение (`legs`, как в AC, или `torso`, как в FM3) — решить при
     реализации, не зафиксировано.
   - Не зафиксировано (решить при реализации): что происходит при попытке
     превысить бюджет — простой запрет экипировки (проще, согласуется с
     «не делать недоделанных фич») или разрешить с штрафом к скорости/AP
     пропорционально перегрузу (больше глубины, как в AC, но и больше
     кода). Для базовой версии по умолчанию — запрет, штраф за перегруз
     можно добавить отдельным пунктом позже, если запрет покажется слишком
     жёстким на плейтесте.
10. **Тюнинг-слайдеры (аналог настройки машины в Gran Turismo).** Зафиксировано
    с пользователем (2026-07-12) как основной фокус кастомизации проекта —
    в отличие от дискретного выбора детали (уже есть, Этап 1 п.3-4), слайдер
    даёт непрерывный параметр с trade-off в обе стороны, настраивается в
    гараже/лоадауте между боями (как и выбор деталей — менять сборку
    посреди боя нельзя, см. Этап 1 п.2). Полноценный UI слайдеров — не в
    HTML-дебаге (см. Этап 1 п.5, полноценный билдер меха — не сюда, это
    Unity), проверять уровнем 0 из AGENTS.md: подставить крайние и среднее
    значение слайдера напрямую в модель и сверить получившиеся статы/урон.
    - **Калибровка оружия** — слайдер на `Weapon`, перераспределяет
      фиксированный «бюджет» между `accuracy` и `damage` (выкрутил в
      точность — потерял в уроне, и наоборот; аналог передаточных чисел в
      GT: короче/длиннее). Веса не расходует, п.9 не касается.
    - **Плотность брони** — слайдер на `Part`, увеличивает добавляемое
      деталью HP ценой добавочного веса (расходует бюджет из п.9). Прямая
      связь с locational damage (Этап 2, п.2): чем плотнее броня детали,
      тем больше у неё HP до уничтожения.
    - Точная формула компромисса (линейная/нелинейная, диапазон слайдера)
      — не зафиксирована, подобрать при реализации и балансировке.
    - Дальнейшие слайдеры (развесовка перед/зад и т.п.) и опциональный
      ресурс перегрева (heat, как в MechWarrior) — сознательно не сюда;
      добавлять отдельными пунктами, если после плейтеста этих двух
      слайдеров захочется больше глубины.
11. **Нейтральный ИИ-страж ценного лута (модель Baron/Roshan из MOBA).**
    Зафиксировано с пользователем (2026-07-12). Это же — решение по «судьбе
    PvE-наследия»: нейтральные ИИ-враги перестают быть автономными
    агрессорами (бродят + агрят ближайшего) и становятся **объективом, на
    который действуют игроки**, а не агентом, который действует на игроков.
    Зачем именно так:
    - **Честность в симметричном PvP.** Автономный агр «на ближайшего» —
      по сути RNG/позиционная лотерея в пользу одной команды, чего в
      server-authoritative модели быть не должно (см. AGENTS.md про честность
      данных). Объектив симметричен: обе команды сами решают, лезть ли, и
      когда.
    - **Осмысленность вместо шума.** Страж стягивает обе команды к одной
      точке карты (ценная деталь под ним) → возникает органическая PvP-драка
      за объектив, а не разрозненные стычки с бродячим мобом.
    - **Кормит стержень игры.** Награда за стража — двойная (вариант «A»,
      выбран с пользователем 2026-07-12): внутриматчевый **бафф** команде +
      редкая **деталь** как мета-вишенка (см. «Экономика награды» ниже).
      Деталь идёт в тюнинг/кастомизацию — ИИ становится воротами к лучшему
      луту, а не отвлечением от неё.
    - **Страж — опциональная цель, а не условие победы.** Зафиксировано с
      пользователем (2026-07-12): победа в катке определяется **победой над
      вражеской командой**, а не убийством стража. Страж — это ставка:
      команда 1 может рискнуть и полезть на стража, а команда 2 — ударить по
      команде 1, пока та завязла на мобе, и команда 1 не выдержит натиск
      стража и врага одновременно. Именно эта опциональность и создаёт
      интересные решения по ходам (то, чего игре сейчас не хватает, чтобы
      быть игрой, а не чередой ходов) — но она же порождает «bait objective»
      проблему, разобранную в «Экономике награды».
    - **Это упрощение ИИ, а не усложнение.** Переиспользуем `Enemy` +
      `SimpleEnemyAI`, но **снимаем** с него «двигаться к ближайшему игроку»
      (см. [src/ai/](src/ai)): страж держит зону, включается только когда
      к нему вошли/ударили (leash/aggro-on-contact). Механика подбора уже
      есть (`OPEN_CHEST` → `Actor.trophies`, Этап 1 п.2) — страж просто
      гейтит сундук (пока страж жив — сундук не вскрыть).

    **Экономика награды (зафиксировано с пользователем 2026-07-12, самое
    тонкое место — читать внимательно перед реализацией).**
    - **Проблема «bait objective».** Раз победа в катке = добить вражескую
      команду, а редкая деталь — это **мета-награда** (полезна после боя, в
      кастомизации, но не в текущей катке), то голая формула «деталь за
      стража» делает нырок на стража чистым минусом по ресурсам здесь и
      сейчас: кто полез — тот потратил AP/HP и подставился, кто ждал — тот
      наказал. Итог «кто напал на стража, тот и проиграл», объектив мёртв.
      В MOBA Барон работает именно потому, что его награда — внутриматчевое
      усиление, помогающее выиграть *текущую* игру; поэтому у нас деталь
      **обязана** сопровождаться внутриматчевым баффом (вариант «A»), иначе
      экономика — чистый downside.
    - **Два разных правила для двух наград — намеренная асимметрия, и она
      несущая:**
      - **Бафф (внутриматчевый) — бинарный, достаётся команде, ДОБившей
        стража** (по last-hit / добивающему удару). Это делает бафф
        «крадущимся»: команда 2 не может просто отсидеться свежей и наказать
        завязшую команду 1 — если она это сделает, команда 1 добьёт стража,
        получит бафф и выиграет последующую стычку. Значит команда 2 тоже
        обязана лезть к стражу (добить/оспорить) → **обе команды тратят
        ресурсы и экспонированы, драка идёт честно у объектива**, а не
        «одни устали об моба, другие приехали свежими» (та самая гравитация
        объектива из «Осмысленности» выше).
      - **Деталь (мета) — вероятностная, доля шанса ∝ вкладу команды в урон
        по стражу.** Снайпить last-hit'ом бессмысленно: подсосать 10% HP и
        добить — значит забрать себе лишь ~10% шанса на деталь, а 90% шанса
        остаётся команде, которая реально снесла 90%. Награждается вложение
        и риск, а не микро-снайп добивания (у нас нет смайт-тайминга MOBA,
        поэтому last-hit-стил детали ощущался бы несправедливо).
    - **Почему асимметрия хороша (богатая вилка решений).** Разные правила
      создают настоящий выбор в момент «страже осталось ~20% HP»: **добить**
      (получить бафф, но подставиться под свежую вторую команду) или
      **отойти** (сохранить свою долю шанса на деталь по вкладу, но отдать
      бафф — и, возможно, всего стража — второй команде). Плюс это
      **наказывает фрилоадера**: «начесал урон и сбежал» сохраняет долю
      шанса на деталь, но бафф отдаёт добившему — халявы нет. Единое правило
      «всё по вкладу» эту вилку убило бы и ослабило давление на команду 2
      лезть в драку.
    - **Формула детали (однозначно, чтобы не читалось двусмысленно).**
      В момент смерти стража, для **каждого игрока** команды независимо:
      `P(деталь) = BASE * (доля_урона_его_команды_по_стражу)`, где
      `BASE ≈ 0.2` (≈ 1 из 5 при 100% вклада — ориентир, не финал).
      - Ролл **независимый на каждого игрока** (поэтому «каждому в команде»
        = у каждого свои ~20% при полном вкладе команды, а не одна коробка
        на всех; один может выбить деталь, другой — нет, красть друг у друга
        нельзя — это гача-разброс, а не внутрикомандный конфликт).
      - Если деталь выпала — её **редкость катается ОТДЕЛЬНЫМ роллом** (вот
        здесь «везение игрока»: у разных игроков могут выпасть разные детали
        разной редкости).
      - Команда с 0 урона по стражу → 0 шанса (халявы за неучастие нет).
      - Пример-проверка: команда A нанесла 90%, команда B добила (10%).
        Деталь: у игроков A по `0.2*0.9 = 0.18`, у игроков B по
        `0.2*0.1 = 0.02`. Бафф: у команды B (добила). Ровно то, чего мы
        хотим — деталь тем, кто вложился; бафф тому, кто рискнул добить.
    - **Реализация — трекинг вклада.** Стражу нужно копить накопленный урон
      по командам (`damage_by_team`, или по актору с последующей сверткой в
      команду) + помнить, кто нанёс добивающий удар. Дешёвое новое
      состояние на сущности стража. Не забыть: урон от овервотча и от
      добивания тоже идёт в вклад.
    - **Пометки на потом (НЕ балансить сейчас, только зафиксировать):**
      - `BASE ≈ 0.2` — это кран мета-прогрессии; быстро/медленно решается
        на Этапе 3 (когда вообще появится накопление деталей в БД), сейчас
        число — только ориентир.
      - **Победа в катке должна оставаться бóльшим краном деталей, чем
        страж**, иначе вернётся вырожденная стратегия «фармить стража,
        забить на победу». Деталь со стража остаётся у команды, даже если
        она проиграла катку (мета персистентна) — это ок как утешение,
        пока победа даёт больше.

    Ключевые решения по балансу (зафиксированы с пользователем 2026-07-12,
    сознательно **против** прямого копирования Барона):
    - **НЕ делаем «чем дольше матч, тем ценнее лут».** У Барона бафф растёт
      со временем и поощряет пассивность — а у нас уже есть овервотч и
      дальний бой, которые и так тянут к кемперству. Складывать три стимула
      к пассивности вместе — вредно. Ценность лута от длительности матча
      **не зависит**.
    - **Отложенный, но фиксированный и заранее известный спавн** (ориентир —
      3-4-й ход, точное число подобрать при балансировке). Инверсия таймера
      Барона: первые ходы — про позиционирование, дальше — гонка за
      объективом. Фиксированный ход (а не случайный) — ради симметричной
      честности: обе команды заранее знают, когда и где появится страж.
    - **Драку решает не «кто первый добежал», а «кто может позволить себе
      вскрыть стража и пережить ответку».** Важный нюанс: чистый забег
      («кто быстрее дошёл и взял») в симметричной игре вырождается в победу
      более быстрого меха без боя (напр. Fireworks, speed 7, просто
      добегает и уходит) — и тогда страж снова «не к месту». PvP создаёт то,
      что убийство стража **дорого** (трата AP/HP/ходов) и оставляет
      команду **потрёпанной** — вторая команда либо перехватывает объектив,
      либо наказывает ослабленных (модель «третьего тела» из Hunt: Showdown).
      Забег остаётся частью решения, но не единственным фактором.
    - **Позиционирование стража на карте — симметричное** (центр / зеркальные
      относительно спавнов точки), чтобы объектив не был coin-flip'ом в
      пользу той команды, что ближе. На ручных картах (Этап 1 п.6) это
      ответственность автора карты, не кода.
    - **Совместимо с обоими исходами п.5** (подбор в бою vs пост-матчевая
      награда): если подбор в бою остаётся — страж гейтит сундук, а деталь
      по формуле выше падает в `trophies` вложившимся; если дроп переезжает
      в пост-матч — та же формула (доля шанса ∝ вкладу) считается по итогам
      боя. Внутриматчевый бафф в обоих случаях выдаётся сразу по добиванию.
      Не завязываемся жёстко ни на один исход заранее.
    - Опциональный анти-стейлмейт-слой (подкрепления / закрывающаяся зона,
      если катка всё же затягивается — модель AI Director из L4D/Vermintide)
      — **сознательно не сюда**, отдельным опциональным пунктом позже, если
      отложенного спавна стража окажется мало против затягивания.
    - Проверять — уровнем 0 из AGENTS.md: собрать `Arena` со стражем у
      сундука, убедиться, что (а) до нужного хода стража нет / он пассивен,
      (б) `OPEN_CHEST` рядом с живым стражем не проходит, а после его смерти
      проходит, (в) страж не уходит с зоны к «ближайшему игроку»,
      (г) бафф достаётся команде, нанёсшей добивающий удар (проверить обеими
      командами как добивающими), (д) доля шанса на деталь считается по
      вкладу: смоделировать 90%/10% урона и сверить `P(деталь)` у игроков
      обеих команд с формулой `BASE * доля_урона` (для детерминизма — задать
      `BASE=1.0` и проверить пропорции, либо замокать RNG).
12. **Ограниченный боезапас дальнобойного оружия.** Зафиксировано с
    пользователем (2026-07-12). Три причины сразу, каждая сама по себе
    достаточна, но именно вместе они и оправдывают механику:
    - **Кастомизация (стержень проекта).** Боезапас — **стат оружия, стоящий
      веса** (расходует бюджет из п.9, тот же принцип, что и плотность брони
      в п.10): «сколько патронов взять» решается **в гараже между боями**,
      это компромисс сборки (больше патронов → тяжелее → меньше
      скорости/брони), а не боевой микроменеджмент. Аналог выбора топлива/
      резины в Gran Turismo — ровно та рамка, в которую проект целится.
    - **Средство против кемперства.** Овервотч и дальний бой уже тянут игру
      к пассивности (та же проблема, что разбиралась у стража, п.11, и у
      слайдеров, п.10). Конечный боезапас — прямой рычаг против этого:
      нельзя вечно сидеть в засаде на дальнобое — патроны тратятся и на
      обычную атаку, и **на выстрел по триггеру овервотча одинаково**
      (это не отдельная более мягкая версия «только овервотч жжёт
      патроны» — обсуждалась, но выбрана полная: боезапас должен влиять на
      стратегию всего матча, не только гасить конкретно кемперов).
    - **Асимметрия ranged/melee.** Ближний бой становится «надёжным, но
      требует сближения», дальний — «мощным, но конечным ресурсом». Это
      усиливает уже заложенную разницу пресетов Fireworks/SteelMan
      (Этап 1, п.8) сверх голых цифр статов: Fireworks — бёрст, пока есть
      патроны, SteelMan — выжидает, когда у дальнобойного противника
      кончится боезапас, и добивает в ближнем бою.
    - **Явно НЕ делаем перезарядку в течение боя** (зафиксировано с
      пользователем 2026-07-12) — пул патронов расходуется на весь матч
      без пополнения; кончились патроны — остаётся только ближний бой
      (если есть) или бесполезное оружие до конца катки. Сознательное
      ограничение объёма боевой бухгалтерии: не вводим `ActionType.RELOAD`
      и связанную с ним трату AP/хода на перезарядку — это уже была бы
      глубина боевой системы, а не кастомизации, чего проект избегает.
    - **Только дальнобойное оружие.** Melee остаётся безлимитным — вопрос
      был явно про дальнобой, ближний бой и так уже «дороже» через
      сближение под огнём.
    - **Модель:** `Weapon.ammo: Optional[int] = None` (`None` = безлимит —
      это значение по умолчанию и для melee-оружия, и как безопасный
      fallback для не тронутых мест кода). Для ranged-оружия в
      [src/mech_presets.py](src/mech_presets.py) задаётся конкретное число.
      Runtime-расход — на самом экземпляре `Weapon` в инвентаре актора
      (mutable поле `current_ammo`, по аналогии с `current_action_points`
      на `Actor`), а не в отдельной структуре — оружие уже не шарится между
      акторами: копируется со свежим `id` на каждого игрока (см. Этап 1,
      п.8, `get_random_mech_preset`/`get_mech_preset_by_name`).
    - **Два хука расхода — оба должны списывать патрон одинаково:**
      `ActionHandler.__perform_action_attack` ([src/action_handler.py](src/action_handler.py),
      обычная атака) и `Game._fire_overwatch_shot` ([src/game.py](src/game.py),
      выстрел по триггеру овервотча). Сейчас это два независимых места
      вызова `weapon.check_hit`/урона — при реализации стоит свести
      проверку/списание патрона в общий хелпер, чтобы не задублировать
      логику и не забыть про один из хуков. Нет патронов (`current_ammo <= 0`
      у оружия с `ammo is not None`) → атака этим оружием недоступна,
      `ActionResult(performed=False, detail=...)`, по аналогии с нехваткой
      `cost_ap`.
    - **Открытый вопрос (решить при реализации):** должна ли постановка
      `ActionType.OVERWATCH` с оружием без патронов быть запрещена сразу
      (в `__perform_action_overwatch`), или разрешить встать в засаду
      «вхолостую» (просто не выстрелит при триггере)? Разрешение вхолостую
      обманчиво для игрока — по умолчанию склоняемся к запрету сразу, но
      не зафиксировано жёстко.
    - **Честность данных.** `current_ammo` оружия — часть `WeaponState`
      (в инвентаре игрока), значит уже подпадает под существующую
      `filter_visible_entities_for_team` в [lobby.py](lobby.py) наравне с
      остальным лоадаутом — не создаёт нового канала утечки, но не забыть
      добавить поле в `WeaponState` (`dto/state.py`), когда будете
      прокидывать `ammo`/`current_ammo` в DTO.
    - **Зависимость на порядок:** делать **после п.9** (весовой бюджет) —
      иначе боезапас нечем оплачивать, и он выродится в голую боевую
      бухгалтерию вместо решения о сборке (та же логика, что уже
      применялась к тюнинг-слайдерам, п.10).
    - Проверять — уровнем 0 из AGENTS.md: собрать `Player` с ranged-оружием
      `ammo=1`, выполнить `Action(type=ATTACK)` дважды — второй раз
      `ActionResult.performed=False` с понятным `detail`; отдельно
      смоделировать овервотч с `ammo=0` на оружии — проверить, что выстрел
      по триггеру не происходит (поведение зависит от решения по открытому
      вопросу выше).
    - Конкретные числа боезапаса на оружие/пресет — не зафиксированы,
      подбирать при плейтесте вместе с балансировкой весового бюджета (п.9).
13. **Открытый вопрос (не решение, направление подумать, 2026-07-13):
    преимущество хода второй командой.** Обнаружено массовыми прогонами
    [balance_sim.py](balance_sim.py): даже в зеркальных матч-апах (идентичные
    составы с обеих сторон) команда, ходящая **второй** в раунде, стабильно
    выигрывает чаще (наблюдалось ~54-61% против ~39-46%). Проверено
    экспериментально — временная перестановка порядка фаз в
    [src/turn.py](src/turn.py) (`Turn.next`/`switch_phase`, обычно
    `TEAM_1_PHASE` → `TEAM_2_PHASE` → `AI_ENEMY_PHASE`) на `TEAM_2_PHASE` →
    `TEAM_1_PHASE` → `AI_ENEMY_PHASE`) перевернула и перекос — значит, дело
    не в асимметрии стартовых точек `map_2`, а именно в порядке ходов
    (первая команда ходит "вслепую" и подставляется, вторая уже реагирует
    на её позицию).
    - **Не фиксируем как проблему прямо сейчас**: это эффект простого
      детерминированного бота (`PvPBotAI` в `balance_sim.py`), который ходит
      по жёсткой эвристике без блефа/выжидания — есть подозрение, что живые
      игроки будут подвержены этому заметно меньше (человек может придержать
      ход, не переть в открытую). Подтвердить/опровергнуть можно только на
      реальном плейтесте с людьми, не ботами.
    - Если на живых игроках перекос всё же будет ощутимым — два кандидата на
      исправление (не взаимоисключающие): (а) рандомизировать, какая команда
      получает `TEAM_1_PHASE` (ходит первой) в начале **каждого матча** —
      простое изменение в `Game`/`Lobby`, не трогает `Turn`; (б) компенсировать
      небольшими правками карты (асимметрия дистанций/укрытий от стартовых
      точек команд на `map_2`), если после (а) второй ходящий всё ещё имеет
      статистическое преимущество при прочих равных.
    - Не делать заранее (до плейтеста с людьми) — рискуем чинить то, что на
      живых игроках может не проявиться вовсе.
14. ✅ **Разброс урона.** — сделано (2026-07-13). Урон был полностью
    детерминирован (`Weapon.damage` — фиксированное число), что ощущалось
    слишком "настольно-механическим"; добавлен случайный разброс на каждое
    попадание, одинаково для ближнего и дальнего оружия.
    - `Weapon.roll_damage()` в [src/entities/base.py](src/entities/base.py):
      `round(damage * uniform(1 - DAMAGE_VARIANCE, 1 + DAMAGE_VARIANCE))`,
      минимум 1. `DAMAGE_VARIANCE = 0.125` (±12.5%, середина запрошенного
      диапазона 10-15%).
    - Заменил прямое использование `weapon.damage` на `weapon.roll_damage()`
      в обоих местах, где считается урон: обычная атака
      (`ActionHandler.__perform_action_attack`,
      [src/action_handler.py](src/action_handler.py)) и выстрел по триггеру
      огневого дозора (`Game._fire_overwatch_shot`,
      [src/game.py](src/game.py)) — раньше это были два независимых расчёта
      урона, важно было не забыть про оба (см. похожую заметку про боезапас
      в п.12 выше).
    - Бонус `melee_power` за ближний бой по-прежнему прибавляется поверх
      результата `roll_damage()` как фиксированное число, без своего
      разброса — рандомизируется только собственно `Weapon.damage`.
    - Известное следствие округления при малых значениях урона: для
      оружия с `damage` в районе 2 разброс ±12.5% (диапазон ~1.75-2.25)
      после округления почти всегда возвращает то же число — т.е.
      слабое оружие (например, "Аварийный клинок" Fireworks, damage=2)
      ощутимого разброса на глаз не даст. Не считаем багом: разброс задан
      как процент, а не абсолютное значение; если для слабого оружия
      разброс тоже должен быть заметен — потребуется отдельная правка
      (например, минимальный абсолютный разброс ±1 вне зависимости от
      процента), не делали её сейчас, т.к. не просили.
    - Баланс-тесты (`balance_sim.py`) сознательно не гонялись сразу после
      этой правки по просьбе пользователя — сначала разброс, потом баланс
      (актуально в первую очередь для новых чисел `StrikeForce`, см. выше).

## Этап 3 — БД и персистентность (PostgreSQL + SQLAlchemy/SQLModel)

Начинать только когда меха/детали уже играбельны и не меняются каждую
неделю — миграции по живой схеме дороже, чем по стабильной.

1. **Инфраструктура.** `docker-compose` с Postgres для локальной
   разработки, Alembic для миграций, SQLModel-модели (хорошо сочетаются с
   уже используемым pydantic-стилем DTO).
2. **Схема БД (минимум):**
   - `Account` — учётка (см. п. 4 про авторизацию).
   - `Pilot` — профиль пилота: `account_id`, `xp`, `level`, `currency`.
   - `Part` — каталог деталей (переезжает из `src/parts_catalog.py` в БД
     как справочник; сид через миграцию).
   - `PilotPartOwnership` — какие детали пилоту доступны/разблокированы
     (не все детали доступны всем сразу).
   - `MechLoadout` — сохранённая сборка: `pilot_id` + ссылки на детали по
     слотам, может быть несколько на пилота.
   - `MatchHistory` — результаты боёв: участники, победитель, полученный
     опыт/валюта/дроп, таймстамп.
   - `Skill` — каталог скиллов (переезжает из `src/skills_catalog.py`,
     Этап 2 п.8, в БД как справочник; сид через миграцию, по аналогии с
     `Part`).
   - `PilotSkillOwnership` — какие скиллы пилот уже открыл через прокачку
     (постоянное «дерево развития» пилота между боями).
3. **Начисление прогрессии.** По окончании `Game` (сейчас `Game.ended`
   в [src/game.py](src/game.py)) — расчёт XP/валюты/дропа и запись в БД.
   Это новый код на границе `Lobby`/`Game`, сама логика боя не меняется.
   - **Скиллы и уровни (продолжение Этапа 2, п.8).** Именно здесь появляется
     то, чего не было в прототипе: реальное начисление XP → рост `Pilot.level`,
     а на каждый новый уровень — **выбор одного скилла из предложенных
     (1 из 2)** по уже зафиксированному статичному дереву (см. Этап 2, п.8:
     уровень 1 без скилла, уровень 2 — «Точный выстрел»/«Усиленный удар»,
     уровень 3 — ветка зависит от выбора на уровне 2). Зафиксировано с
     пользователем (2026-07-10): **само меню выбора рисует Unity-клиент**,
     не HTML-дебаг. Сервер отвечает за данные и правила, а не за UI:
     - при апе уровня по дереву определяет доступные на выбор 2 узла
       (для уровня 3 — по ветке, выбранной на уровне 2, т.е. нужно
       учитывать предпосылку/родительский узел, а не только `level`);
     - принимает выбор игрока и фиксирует его в `PilotSkillOwnership`;
     - выбранные скиллы пилота при старте боя грузятся из БД в
       `Player.skills` (в прототипе Этапа 2 они были захардкожены) —
       дальше боевой хук прока из п.8 работает без изменений.
     - Эндпоинты под это добавляются в п.5 ниже (профиль/уровень/скиллы).
4. **Авторизация — предложение, не блокирует остальное.**
   Сейчас клиент сам генерирует `player_id` (UUID) без какой-либо проверки.
   Рекомендация: на первом шаге БД **не делать полноценный логин/пароль**,
   а завести `Account`, привязанный к тому же клиентскому UUID (по сути —
   анонимный профиль с постоянным ID). Полноценную авторизацию
   (email/pass или OAuth) — отдельным этапом, когда появится реальный
   Unity-клиент и вопрос "как не дать украсть чужой прогресс" станет
   насущным. Если для тебя это принципиально раньше — можно поднять
   приоритет, но по умолчанию предлагаю не блокировать этим Этап 3.
5. **Эндпоинты:** профиль пилота (статы/XP/уровень), список деталей
   пилота, CRUD лоадаутов, история матчей, а также скиллы пилота: открытые
   скиллы + доступный на текущем уровне выбор и приём выбранного скилла
   (см. п.3, «Скиллы и уровни»).
6. **`lobby.py`/`start_game`:** лоадаут игрока берётся из БД
   (`MechLoadout` по умолчанию или выбранный) вместо каталога в коде.
   Каталог деталей в коде (Этап 1, п. 4) на этом этапе выпиливается —
   переезжает в БД полностью.

## Этап 4 — сессии, устойчивость, продакшен (после того как есть БД)

Не расписываю подробно, чтобы не планировать слишком далеко вперёд —
детали будут яснее после Этапа 3. Крупными мазками:

1. Токены сессии вместо доверия к клиентскому `player_id` — нужно решить
   вместе с вопросом авторизации из Этапа 3.
2. Переподключение/устойчивость к разрыву соединения — уже частично есть
   (`websocket_endpoint` шлёт текущее состояние при реконнекте); можно
   опереться на существующий dump/restore
   ([game_state_utils.py](game_state_utils.py)) для восстановления после
   падения процесса, а не только реконнекта клиента.
3. Матчмейкинг вместо ручного шаринга лобби — опционально, по необходимости.
   Если к этому моменту решится открытый вопрос про score силы пилота/сборки
   (см. Этап 2, п.8, «Открытый вопрос... про шанс прока скиллов») — этот же
   score естественно использовать и здесь, для подбора соперников
   сопоставимой силы.
4. Деплой/инфраструктура — отдельное обсуждение, когда дойдём до этой точки.

## Что намеренно не планируем сейчас

- Полноценный UI сборки меха в HTML-клиенте — не тратим время, целевой
  клиент Unity.
- Процедурную генерацию карт — не развиваем, но и не обязаны прямо сейчас
  удалять, если не мешает.
- Полноценную авторизацию — до тех пор, пока анонимного постоянного ID
  достаточно для тестирования прогрессии.
